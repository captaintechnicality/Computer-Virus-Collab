sprite_index = asset_get("empty_sprite");

if (boss = 6 && (state = PS_IDLE || state = PS_IDLE_AIR)){
	sprite_index = sprite_get( "undertale_idle" );
}

if (boss = 0){
	sprite_index = sprite_get("empty_sprite");
}